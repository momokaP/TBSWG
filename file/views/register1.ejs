<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>사용자 등록</title>
    <link rel="stylesheet" href="css/login_resiger.css">
</head>
<body>
    <header class="border">
        <div class="container ">
          <nav>
            <a href="/" class="logo">TBSWB</a>
          </nav>
        </div>
      </header>

    <main id="site-main"> 
        <div class="card">
            <div class="card-header">
                <h1 class="card-title">사용자 등록</h1>
                <p class="card-description">계정에 로그인하세요.</p>
            </div>
            <form id="registerForm" action="/register" method="POST" class="register">
                <div class="card-content">  
                    <div class="form-group">
                        <label for="username">아이디</label>
                        <input type="text" id="username" name="username" placeholder="아이디" >
                    </div>
                    <div class="form-group">
                        <label for="password">비밀번호</label>
                        <input type="password" id="password" name="password" placeholder="비밀번호" >
                    </div>
                    <div class="form-group">
                        <label for="confirmPassword">비밀번호 확인</label>
                        <input type="password" id="confirmPassword" name="confirmPassword" placeholder="비밀번호 확인" >
                    </div>
                    <p id="error" class="error" style="display: none;">아이디, 비밀번호, 비밀번호 확인을 모두 입력해주세요.</p>            
                </div>
                <div class="card-footer">
                    <button class="button" type="submit">등록</button>
                </div>
            </form>
            </div>
        </div>
    </main>

    <script>
        document.getElementById('registerForm').addEventListener('submit', function(event) {
          // 아이디, 비밀번호, 비밀번호 확인 입력값 가져오기
          var username = document.getElementById('username').value;
          var password = document.getElementById('password').value;
          var confirmPassword = document.getElementById('confirmPassword').value;

          // 아이디, 비밀번호, 비밀번호 확인이 비어있는지 확인
          if (!username || !password || !confirmPassword ) {
            event.preventDefault(); // 폼 제출을 막음
            var errorMessage = document.getElementById('error');
            errorMessage.classList.add('show'); // 오류 메시지를 보이도록 설정
          }
        });
      </script>
</body>
</html>